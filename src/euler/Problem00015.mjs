import { CheckSolution } from "./CheckSolution.mjs";

/**
 * https://projecteuler.net/problem=15
 * 
 * Note that this is more about math than programming by far. Indeed if understanding the problem in its combinatorics version there is a closed formula for it (thought it cannot directly expressed in JavaScript, neeeding the faculty function)
 */

let solution = undefined; // put the solution (answer) here.

console.log(CheckSolution.check(15, solution));