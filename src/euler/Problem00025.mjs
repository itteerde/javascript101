import { CheckSolution } from "./CheckSolution.mjs";

/**
 * 1000-digit Fibonacci Number
 * 
 * BigInt avoids implementing adding with carry on strings representing integers -- and makes it really easy.
 * 
 * https://projecteuler.net/problem=25
 */

let solution = undefined; // put the solution (answer) here.

console.log(CheckSolution.check(25, solution));