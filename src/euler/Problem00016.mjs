import { CheckSolution } from "./CheckSolution.mjs";

/**
 * https://projecteuler.net/problem=16
 * 
 * Note that this is not possible dirctly with Number, as its real number representation will lost the precision.
 */

let solution = 1366; // put the solution (answer) here.

console.log(CheckSolution.check(16, solution));