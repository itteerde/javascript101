import { CheckSolution } from "./CheckSolution.mjs";

/**
 * Counting Sundays
 * 
 * Date does actually work with negative numbers for dates prior to 1970, at least for four digit years, so it becomes easy(ish). Good exercise to explore Date, otherwise useless.
 * 
 * https://projecteuler.net/problem=19
 */

let solution = undefined; // put the solution (answer) here.

console.log(CheckSolution.check(19, solution));