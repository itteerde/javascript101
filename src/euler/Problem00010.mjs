import { CheckSolution } from "./CheckSolution.mjs";

/**
 * Summation of Primes
 * 
 * Naively this is easy, but is it still fast enough?. Damn those computers, just need the very first insight to make if (very much) fast enough. No sieve required.
 * 
 * https://projecteuler.net/problem=10
 */

let solution = undefined; // put the solution (answer) here.

console.log(CheckSolution.check(10, solution));