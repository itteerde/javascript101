import { CheckSolution } from "./CheckSolution.mjs";

/**
 * https://projecteuler.net/problem=17
 * 
 * Only do this is the puzzle sounds interesting, having to implement the ways the spoken language works. It might be nice to get some appreciation how one has to work with the oddities of natural language when processing it programatically, but otherwise, not very interesting, just complicated.
 */

let solution = undefined; // put the solution (answer) here.

console.log(CheckSolution.check(17, solution));